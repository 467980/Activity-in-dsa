// use for loop its because you know how many you gonna loop
for (let i = 1; i <= 10; i++) {//let i stat at 1 end at 10
    if (i % 2 === 0) {//use modulo to start the for loop at 3 === to if the same value
        console.log(i);
    }
}
